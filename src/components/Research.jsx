import { motion } from 'motion/react'
const view = { once: true, amount: .2 }
export default function Research({ t }) { return <section id="research" className="section research-section"><motion.div className="section-intro" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={view} transition={{ duration: .8 }}><p className="section-kicker">{t.research.kicker}</p><h2>{t.research.heading}</h2></motion.div><div className="research-grid">{t.research.items.map((item, i) => <motion.article className="research-card" key={item.number} initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={view} transition={{ duration: .75, delay: i * .08 }}><span>{item.number}</span><h3>{item.title}</h3><p>{item.description}</p><div>{item.flow}</div></motion.article>)}</div></section> }
