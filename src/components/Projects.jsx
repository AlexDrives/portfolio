import { motion } from 'motion/react'
import { projects } from '../data/projects'
import ProjectCard from './ProjectCard'
export default function Projects() { return <section id="projects" className="section projects-section"><motion.div className="section-intro split" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .8 }}><div><p className="section-kicker">Selected Projects / 03</p><h2>Experiments in representation, perception, and decision-making.</h2></div><a className="github-link" href="https://github.com/AlexDrives" target="_blank" rel="noopener noreferrer">AlexDrives on GitHub <span>↗</span></a></motion.div><div className="projects-grid">{projects.map((project, i) => <ProjectCard project={project} index={i} key={project.title} />)}</div></section> }
