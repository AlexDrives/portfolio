import { motion } from 'motion/react'
import ProjectCard from './ProjectCard'
export default function Projects({ t }) { return <section id="projects" className="section projects-section"><motion.div className="section-intro split" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .8 }}><div><p className="section-kicker">{t.projects.kicker}</p><h2>{t.projects.heading}</h2></div><a className="github-link" href="https://github.com/AlexDrives" target="_blank" rel="noopener noreferrer">{t.projects.github} <span>↗</span></a></motion.div><div className="projects-grid">{t.projects.items.map((project, i) => <ProjectCard project={project} index={i} key={project.title} t={t} />)}</div></section> }
