import { motion } from 'motion/react'
import { experience } from '../data/experience'
export default function Experience() { return <section id="experience" className="section experience-section"><motion.div className="section-intro compact" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .8 }}><p className="section-kicker">Experience / 02</p><h2>Working toward capable systems that meet the physical world.</h2></motion.div><div className="experience-list">{experience.map((item, i) => <motion.a href={item.url} className="experience-card" key={item.title} initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .75, delay: i * .1 }}><div className="card-index">0{i + 1}</div><div className="experience-main"><p className="card-meta">{item.organization} <span>·</span> {item.date}</p><h3>{item.title}</h3><p>{item.description}</p><div className="tags">{item.tags.map(tag => <span key={tag}>{tag}</span>)}</div></div><span className="card-arrow">↗</span></motion.a>)}</div></section> }
