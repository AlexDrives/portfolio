import { motion } from 'motion/react'
export default function Experience({ t, onOpenExperience }) { return <section id="experience" className="section experience-section"><motion.div className="section-intro compact" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .8 }}><p className="section-kicker">{t.experience.kicker}</p><h2>{t.experience.heading}</h2></motion.div><div className="experience-list">{t.experience.items.map((item, i) => <motion.a href={`#experience/${item.slug}`} onClick={onOpenExperience} className="experience-card" key={item.title} initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true }} transition={{ duration: .75, delay: i * .1 }}><div className="card-index">0{i + 1}</div><div className="experience-main"><p className="card-meta">{item.organization} <span>·</span> {item.date}</p><h3>{item.title}</h3><p>{item.description}</p><div className="tags">{item.tags.map(tag => <span key={tag}>{tag}</span>)}</div><span className="card-detail-hint">{t.experience.read} ↗</span></div><span className="card-arrow">↗</span></motion.a>)}</div></section> }
