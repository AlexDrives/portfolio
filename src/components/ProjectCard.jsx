import { motion } from 'motion/react'
export default function ProjectCard({ project, index, t }) { return <motion.a className="project-card" href={project.url} target="_blank" rel="noopener noreferrer" initial={{ y: 24, opacity: 0 }} whileInView={{ y: 0, opacity: 1 }} viewport={{ once: true, amount: .15 }} transition={{ duration: .75, delay: index * .09 }} whileHover={{ y: -3 }}><div className="project-top"><span>{t?.projects?.kicker?.split(' / ')[0] || 'Project'} / 0{index + 1}</span><span>{project.date}</span></div><div className="project-body"><h3>{project.title}</h3><p>{project.description}</p></div><div className="project-bottom"><div className="tags">{project.tags.map(tag => <span key={tag}>{tag}</span>)}</div><span className="external-arrow">↗</span></div>{project.metrics && <div className="metrics">{project.metrics.map(metric => <span key={metric}>{metric}</span>)}</div>}</motion.a> }
